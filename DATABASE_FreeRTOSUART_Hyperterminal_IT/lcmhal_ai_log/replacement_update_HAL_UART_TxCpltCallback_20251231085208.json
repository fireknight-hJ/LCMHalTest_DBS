{
  "function_name": "HAL_UART_TxCpltCallback",
  "replacement_code": "void HAL_UART_TxCpltCallback(UART_HandleTypeDef *huart)\n{\n    /* UART TX complete callback - in emulation, we can just return */\n    /* This is typically called from interrupt context */\n    return;\n}",
  "reason": "UART TX complete callback function. In emulation, we don't have real hardware interrupts, so we can return without performing any hardware-dependent operations."
}