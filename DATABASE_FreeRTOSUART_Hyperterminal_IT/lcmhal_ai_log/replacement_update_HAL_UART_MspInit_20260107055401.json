{
  "function_name": "HAL_UART_MspInit",
  "replacement_code": "/**\n* @brief UART MSP Initialization\n*        This function configures the hardware resources used in this example:\n*           - Peripheral's clock enable\n*           - Peripheral's GPIO Configuration\n*           - NVIC configuration for UART interrupt request enable\n* @param huart: UART handle pointer\n* @retval None\n*/\nvoid HAL_UART_MspInit(UART_HandleTypeDef *huart)\n{  \n  /* In emulation, we don't need to configure actual hardware clocks or GPIOs */\n  /* But we preserve the interrupt configuration for proper emulation */\n  \n  /*##-3- Configure the NVIC for UART ########################################*/   \n  /* NVIC for USARTx */\n  HAL_NVIC_SetPriority(USARTx_IRQn, 0, 1);\n  HAL_NVIC_EnableIRQ(USARTx_IRQn);\n}",
  "reason": "Simplified HAL_UART_MspInit for emulation by removing hardware-specific clock and GPIO configuration. In emulation, we don't need to enable actual hardware clocks or configure physical GPIO pins, but we preserve the interrupt configuration to maintain proper interrupt handling behavior."
}