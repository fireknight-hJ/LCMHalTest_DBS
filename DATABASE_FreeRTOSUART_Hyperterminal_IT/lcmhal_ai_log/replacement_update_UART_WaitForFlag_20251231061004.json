{
  "function_name": "UART_WaitForFlag",
  "replacement_code": "void UART_WaitForFlag(UART_Type *base, uint32_t flag, FlagStatus status, uint32_t timeout)\n{\n    /* Skip waiting for hardware flags in emulation */\n    /* This function would normally wait for UART status flags */\n    \n    (void)base;\n    (void)flag;\n    (void)status;\n    (void)timeout;\n    \n    /* Immediately return without waiting */\n    return;\n}",
  "reason": "Function that waits for UART hardware flags. This is a classic LOOP type function that causes dead loops in emulation. The replacement immediately returns without waiting for hardware flags."
}