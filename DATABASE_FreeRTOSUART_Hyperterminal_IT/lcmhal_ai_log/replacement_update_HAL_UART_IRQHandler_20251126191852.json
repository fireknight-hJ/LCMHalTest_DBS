{
  "function_name": "HAL_UART_IRQHandler",
  "replacement_code": "void HAL_UART_IRQHandler(UART_HandleTypeDef *huart)\n{\n  /* Simulate interrupt handling by calling appropriate callback functions */\n  \n  /* For receiver data ready scenario - use RxState to determine callback */\n  if (huart->RxState == HAL_UART_STATE_BUSY_RX)\n  {\n    /* Call receive complete callback */\n#if (USE_HAL_UART_REGISTER_CALLBACKS == 1)\n    if (huart->RxCpltCallback != NULL)\n    {\n      huart->RxCpltCallback(huart);\n    }\n#else\n    HAL_UART_RxCpltCallback(huart);\n#endif /* USE_HAL_UART_REGISTER_CALLBACKS */\n    return;\n  }\n\n  /* For transmission scenarios - use gState to determine callback */\n  if (huart->gState == HAL_UART_STATE_BUSY_TX)\n  {\n    /* Call transmit complete callback */\n#if (USE_HAL_UART_REGISTER_CALLBACKS == 1)\n    if (huart->TxCpltCallback != NULL)\n    {\n      huart->TxCpltCallback(huart);\n    }\n#else\n    HAL_UART_TxCpltCallback(huart);\n#endif /* USE_HAL_UART_REGISTER_CALLBACKS */\n    return;\n  }\n\n  /* For error scenarios - call error callback */\n  if (huart->ErrorCode != HAL_UART_ERROR_NONE)\n  {\n#if (USE_HAL_UART_REGISTER_CALLBACKS == 1)\n    /*Call registered error callback*/\n    if (huart->ErrorCallback != NULL)\n    {\n      huart->ErrorCallback(huart);\n    }\n#else\n    /*Call legacy weak error callback*/\n    HAL_UART_ErrorCallback(huart);\n#endif /* USE_HAL_UART_REGISTER_CALLBACKS */\n    return;\n  }\n\n  /* For IDLE event scenarios */\n  if (huart->ReceptionType == HAL_UART_RECEPTION_TOIDLE)\n  {\n    uint16_t nb_rx_data = huart->RxXferSize - huart->RxXferCount;\n    if ((huart->RxXferCount > 0U) && (nb_rx_data > 0U))\n    {\n      huart->RxEventType = HAL_UART_RXEVENT_IDLE;\n#if (USE_HAL_UART_REGISTER_CALLBACKS == 1)\n      /*Call registered Rx Event callback*/\n      if (huart->RxEventCallback != NULL)\n      {\n        huart->RxEventCallback(huart, nb_rx_data);\n      }\n#else\n      /*Call legacy weak Rx Event callback*/\n      HAL_UARTEx_RxEventCallback(huart, nb_rx_data);\n#endif /* (USE_HAL_UART_REGISTER_CALLBACKS) */\n      return;\n    }\n  }\n\n  /* For wakeup scenarios */\n#if defined(USART_CR1_UESM)\n#if defined(USART_CR3_WUFIE)\n#if (USE_HAL_UART_REGISTER_CALLBACKS == 1)\n    /* Call registered Wakeup Callback */\n    if (huart->WakeupCallback != NULL)\n    {\n      huart->WakeupCallback(huart);\n    }\n#else\n    /* Call legacy weak Wakeup Callback */\n    HAL_UARTEx_WakeupCallback(huart);\n#endif /* USE_HAL_UART_REGISTER_CALLBACKS */\n    return;\n#endif /* USART_CR3_WUFIE */\n#endif /* USART_CR1_UESM */\n\n  /* For transmission end scenarios */\n  UART_EndTransmit_IT(huart);\n}",
  "reason": "Removed references to non-existent RxISR and TxISR fields, replaced with state-based callback logic"
}