{
  "function_name": "HAL_FLASHEx_Erase",
  "replacement_code": "HAL_StatusTypeDef HAL_FLASHEx_Erase(FLASH_EraseInitTypeDef *pEraseInit, uint32_t *SectorError)\n{\n  /* Check the parameters */\n  assert_param(IS_FLASH_TYPEERASE(pEraseInit->TypeErase));\n  \n  /* For emulation, simulate successful erase operation */\n  /* In real hardware, this would perform FLASH memory erase */\n  /* In POSIX environment, we can just return success */\n  \n  if (SectorError != NULL)\n  {\n    *SectorError = 0xFFFFFFFFU; /* No sector error */\n  }\n  \n  return HAL_OK;\n}",
  "reason": "Replace HAL_FLASHEx_Erase to remove hardware-dependent FLASH erase operations. The function normally performs FLASH memory erase operations with direct peripheral register access, but in emulation we can simulate successful erase."
}