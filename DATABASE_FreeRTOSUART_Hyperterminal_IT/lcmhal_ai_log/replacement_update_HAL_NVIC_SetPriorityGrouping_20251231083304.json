{
  "function_name": "HAL_NVIC_SetPriorityGrouping",
  "replacement_code": "void HAL_NVIC_SetPriorityGrouping(uint32_t PriorityGroup)\n{\n    /* NVIC priority grouping setup */\n    /* In emulation, we can skip hardware register writes */\n    /* but preserve the parameter for compatibility */\n    \n    /* No hardware operations in emulation */\n    (void)PriorityGroup; /* Avoid unused parameter warning */\n    \n    return;\n}",
  "reason": "This is an INIT type function that sets up interrupt priorities. In emulation, we can skip the hardware register writes while preserving the function signature for compatibility."
}