{
  "function_name": "HAL_NVIC_SetPriorityGrouping",
  "replacement_code": "void HAL_NVIC_SetPriorityGrouping(uint32_t PriorityGroup)\n{\n    /* NVIC priority grouping setup */\n    /* In emulation, we skip hardware register writes */\n    /* This is typically a simple register write without loops */\n    \n    /* Preserve any software state management if present */\n    return;\n}",
  "reason": "HAL_NVIC_SetPriorityGrouping is called from BSP_LED_Init. While this function likely doesn't contain loops, it's a hardware-dependent function that should be replaced to avoid MMIO operations. Classified as RETURNOK type."
}