{
  "function_name": "HAL_UART_AbortReceive_IT",
  "replacement_code": "HAL_StatusTypeDef HAL_UART_AbortReceive_IT(UART_HandleTypeDef *huart)\n{\n  /* Skip all hardware-specific interrupt disable operations */\n  /* Original: ATOMIC_CLEAR_BIT(huart->Instance->CR1, (USART_CR1_RXNEIE | USART_CR1_PEIE)); */\n  /* Original: ATOMIC_CLEAR_BIT(huart->Instance->CR3, USART_CR3_EIE); */\n\n  /* If Reception till IDLE event was ongoing, skip IDLEIE interrupt disable */\n  if (huart->ReceptionType == HAL_UART_RECEPTION_TOIDLE)\n  {\n    /* Skip hardware-specific atomic operation */\n    /* Original: ATOMIC_CLEAR_BIT(huart->Instance->CR1, (USART_CR1_IDLEIE)); */\n  }\n\n  /* Skip DMA disable and abort operations */\n  /* Original code had hardware register checks and DMA abort calls */\n  \n  /* Reset Rx transfer counter */\n  huart->RxXferCount = 0x00U;\n\n  /* Restore huart->RxState to Ready */\n  huart->RxState = HAL_UART_STATE_READY;\n  huart->ReceptionType = HAL_UART_RECEPTION_STANDARD;\n\n  /* Call user Abort complete callback */\n#if (USE_HAL_UART_REGISTER_CALLBACKS == 1)\n  if (huart->AbortReceiveCpltCallback != NULL)\n  {\n    huart->AbortReceiveCpltCallback(huart);\n  }\n#else\n  HAL_UART_AbortReceiveCpltCallback(huart);\n#endif /* USE_HAL_UART_REGISTER_CALLBACKS */\n\n  return HAL_OK;\n}",
  "reason": "Fixed HAL_UART_AbortReceive_IT to remove hardware dependencies. The function contains ATOMIC_CLEAR_BIT operations that use ARM-specific atomic instructions which can cause issues in emulation. The new implementation:\n1. Removes all ATOMIC_CLEAR_BIT operations that access hardware registers\n2. Skips DMA-related hardware operations\n3. Preserves the state management functionality (resetting counters and states)\n4. Calls the appropriate abort completion callback\n5. Returns HAL_OK to maintain compatibility\nThis allows the function to work correctly in emulation without hardware dependencies and prevents potential issues with atomic operations."
}