{
  "function_name": "HAL_RCC_OscConfig",
  "replacement_code": "__weak HAL_StatusTypeDef HAL_RCC_OscConfig(RCC_OscInitTypeDef  *RCC_OscInitStruct)\n{\n  /* Check Null pointer */\n  if (RCC_OscInitStruct == NULL)\n  {\n    return HAL_ERROR;\n  }\n\n  /* Check the parameters */\n  assert_param(IS_RCC_OSCILLATORTYPE(RCC_OscInitStruct->OscillatorType));\n\n  /* In POSIX environment, we assume all oscillator configurations succeed immediately */\n  /* Skip all hardware-specific operations and timeout loops */\n  \n  return HAL_OK;\n}",
  "reason": "Replace hardware-specific RCC oscillator configuration with simplified version that returns success immediately in POSIX environment"
}