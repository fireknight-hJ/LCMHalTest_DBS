{
  "function_name": "HAL_InitTick",
  "replacement_code": "/**\n* @brief  This function configures the TIM6 as a time base source.\n*         The time source is configured to have 1ms time base with a dedicated\n*         Tick interrupt priority.\n* @note   This function is called  automatically at the beginning of program after\n*         reset by HAL_Init() or at any time when clock is configured, by HAL_RCC_ClockConfig().\n* @param  TickPriority: Tick interrupt priority.\n* @retval HAL status\n*/\nHAL_StatusTypeDef HAL_InitTick (uint32_t TickPriority)\n{\n  /* In emulation, simply return success without hardware timer configuration */\n  return HAL_OK;\n}",
  "reason": "Simplified HAL_InitTick to return HAL_OK immediately for emulation. This removes all hardware timer configuration (TIM6) and clock frequency calculations that depend on real hardware peripherals."
}