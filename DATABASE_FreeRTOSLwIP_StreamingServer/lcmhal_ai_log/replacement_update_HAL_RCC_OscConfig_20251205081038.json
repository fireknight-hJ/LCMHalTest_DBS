{
  "function_name": "HAL_RCC_OscConfig",
  "replacement_code": "HAL_StatusTypeDef HAL_RCC_OscConfig(RCC_OscInitTypeDef  *RCC_OscInitStruct)\n{\n  /* Check Null pointer */\n  if (RCC_OscInitStruct == NULL)\n  {\n    return HAL_ERROR;\n  }\n\n  /* Check the parameters */\n  assert_param(IS_RCC_OSCILLATORTYPE(RCC_OscInitStruct->OscillatorType));\n\n  /* Skip all hardware-dependent operations */\n  /* Simulate successful oscillator configuration */\n  \n  return HAL_OK;\n}",
  "reason": "The original function contains hardware-dependent wait loops that check for oscillator ready flags. Since MMIO operations have been replaced with stubs, these flags will never be set, causing infinite loops. This replacement removes all hardware-dependent operations while preserving parameter validation and returning success to allow the program to continue."
}