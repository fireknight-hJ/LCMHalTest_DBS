{
  "function_name": "HAL_PWREx_EnableOverDrive",
  "replacement_code": "/**\n* @brief  Activates the Over-Drive mode.\n* @note   This mode allows the CPU and the core logic to operate at a higher frequency\n*         than the normal mode for a given voltage scaling (scale 1, scale 2 or scale 3).\n* @note   It is recommended to enter or exit Over-drive mode when the application is not running\n*         critical tasks and when the system clock source is either HSI or HSE.\n*         During the Over-drive switch activation, no peripheral clocks should be enabled.\n*         The peripheral clocks must be enabled once the Over-drive mode is activated.\n* @retval HAL status\n*/\nHAL_StatusTypeDef HAL_PWREx_EnableOverDrive(void)\n{\n  /* In emulation, we assume Over-Drive mode is always successfully enabled */\n  return HAL_OK;\n}",
  "reason": "Simplified HAL_PWREx_EnableOverDrive for emulation:\n1. Removed all hardware-dependent operations (register accesses, flag checks, while loops)\n2. Returns HAL_OK immediately to indicate successful operation\n3. In emulation, we don't need actual hardware power management features"
}